## Dependencias

```xml
<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi</artifactId>
    <version>4.1.2</version>
</dependency>
<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi-ooxml</artifactId>
    <version>4.1.2</version>
</dependency>
```

## Workbook

Workbook interface is an high level representation of a Excel workbook. This is the first object most users will construct whether they are reading or writing a workbook

### Workbook implementations

* HSSFWorkbook (xls)
* XSSFWorkbook (xlsx)
* SXSSFWorkbook (xlsx) para excels de gran tamaño, no carga toda la excel en memoria.
